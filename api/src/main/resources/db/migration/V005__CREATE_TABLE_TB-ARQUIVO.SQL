/*
	Objetivo:						Modelar tabela de Arquivos
	Autor:							José Quintino
	Criação: 						24/01/2026
	Tarefa: 						SEPLAGMT24012026122745API
*/

CREATE TABLE IF NOT EXISTS TB_ARQUIVO (
    CODE BIGSERIAL NOT NULL,
    CODE_PUBLIC UUID NOT NULL DEFAULT GEN_RANDOM_UUID(),
    NOME VARCHAR (255) NOT NULL,
    TAMANHO VARCHAR (255) NOT NULL,
    EXTENSAO VARCHAR (255) NOT NULL,
    ENDERECO VARCHAR (255) NOT NULL,
    CREATED_AT TIMESTAMP NOT NULL DEFAULT NOW(),
    UPDATED_AT TIMESTAMP NULL,
    DELETED_AT TIMESTAMP NULL,
    IS_ACTIVE BOOLEAN NOT NULL DEFAULT TRUE,
    CONSTRAINT PK_ARQUIVO PRIMARY KEY (CODE),
    CONSTRAINT UN_ARQUIVO UNIQUE (NOME, TAMANHO, EXTENSAO)
);
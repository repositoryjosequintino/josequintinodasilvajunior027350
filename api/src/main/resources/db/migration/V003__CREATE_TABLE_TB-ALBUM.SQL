/*
	Objetivo:						Modelar tabela de Álbum
	Autor:							José Quintino
	Criação: 						23/01/2026
	Tarefa: 						SEPLAGMT23012026183442API
*/

CREATE TABLE IF NOT EXISTS TB_ALBUM (
	CODE BIGSERIAL NOT NULL,
	CODE_PUBLIC UUID NOT NULL DEFAULT GEN_RANDOM_UUID(),
	TITULO VARCHAR(100) NOT NULL,
	CREATED_AT TIMESTAMP NOT NULL DEFAULT NOW(),
	UPDATED_AT TIMESTAMP NULL,
	DELETED_AT TIMESTAMP NULL,
	IS_ACTIVE BOOLEAN NOT NULL DEFAULT TRUE,
	CONSTRAINT PK_ALBUM PRIMARY KEY (CODE),
	CONSTRAINT UN_ALBUM UNIQUE (TITULO)
);
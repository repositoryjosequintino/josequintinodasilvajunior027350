/*
	Objetivo:						Modelar tabela de Artista
	Autor:							José Quintino
	Criação: 						23/01/2026
	Tarefa: 						SEPLAGMT23012026183442API
*/

CREATE TABLE IF NOT EXISTS TB_ARTISTA (
	CODE BIGSERIAL NOT NULL,
	CODE_PUBLIC UUID NOT NULL DEFAULT GEN_RANDOM_UUID(),
	NOME VARCHAR(100) NOT NULL,
	CREATED_AT TIMESTAMP NOT NULL DEFAULT NOW(),
	UPDATED_AT TIMESTAMP NULL,
	DELETED_AT TIMESTAMP NULL,
	IS_ACTIVE BOOLEAN NOT NULL DEFAULT TRUE,
	CONSTRAINT PK_ARTISTA PRIMARY KEY (CODE),
	CONSTRAINT UN_ARTISTA UNIQUE (NOME)
);